machine:
  node:
    version: 8.9.0
dependencies:
  override:
    - yarn
test:
  override:
    - yarn test
    - yarn eslint ./src/**/*.js
deployment:
  master:
    branch: master
    commands:
      - yarn build
      - ./node_modules/.bin/firebase deploy --token "$FIREBASE_DEPLOY_TOKEN" --non-interactive